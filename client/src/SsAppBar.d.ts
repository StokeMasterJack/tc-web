import { ReactNode } from 'react';
export declare function HomeButton(): JSX.Element;
export default function ({ left, title, actions, home }: {
    left?: ReactNode;
    title?: ReactNode;
    actions?: ReactNode | ReactNode[];
    home?: ReactNode;
}): JSX.Element;
